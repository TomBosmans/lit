{
  "version": "1.0.109",
  "name": "lit",
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {
    "awilix": "^8.0.1",
    "hono": "^2.2.5",
    "rome": "^12.1.3"
  },
  "scripts": {
    "start:watch": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "format": "bun run rome check --apply .",
    "lint": "bun run rome format --write ."
  },
  "module": "src/index.js"
}
